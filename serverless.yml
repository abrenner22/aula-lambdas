service: consultar-processos

frameworkVersion: '>=3.0.0'
provider:
  name: aws
  runtime: python3.11
  region: us-east-1
  stage: ${opt:stage, 'dev'}

functions:
  hello:
    handler: handler.lambda_handler
    events:
      - http:
          path: processos
          method: get

package:
  exclude:
    - node_modules/**
    - .gitignore
    - .github/**
    - README.md
    - deploy.yml